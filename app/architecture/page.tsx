import React from "react";

export default function Architecture() {
  return (
    <main
      style={{
        padding: "2.5rem",
        maxWidth: "960px",
        margin: "0 auto",
        fontFamily:
          "system-ui, -apple-system, BlinkMacSystemFont, sans-serif",
      }}
    >
      <h1>Proof of Care Architecture</h1>

      <p>
        This page describes how Proof of Care is implemented as a verifiable,
        composable, and value-generating protocol layer.
      </p>
    </main>
  );
}
